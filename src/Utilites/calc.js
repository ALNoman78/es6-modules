const add = (num1 , num2) => {
    return num1 + num2
}

const multi = (first , second) => {
    return first * second
}
export {add , multi}

// const disc = [add , multi] // this method also work for another component
// export default add;